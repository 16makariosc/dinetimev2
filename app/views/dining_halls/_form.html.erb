<%= form_for(@dining_hall) do |f| %>
  <% if @dining_hall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dining_hall.errors.count, "error") %> prohibited this dining_hall from being saved:</h2>

      <ul>
      <% @dining_hall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
